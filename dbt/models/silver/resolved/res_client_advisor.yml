version: 2
models:
  - name: res_client_advisor
    description: "Relationship: which advisor manages which client (temporal)"
    columns:
      - name: client_advisor_id
        tests: [unique, not_null]
      - name: client_id
        tests: [not_null]
      - name: advisor_id
        tests: [not_null]
      - name: is_current
        tests: [not_null]

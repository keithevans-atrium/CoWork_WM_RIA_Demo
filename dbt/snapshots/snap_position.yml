snapshots:
  - name: snap_position
    description: "SCD Type 2 history for position changes (quantity, price, market value, cost basis)"
    relation: ref('raw_position')
    config:
      schema: SNAPSHOTS
      unique_key: record_pk
      strategy: check
      check_cols:
        - change_key
      hard_deletes: new_record

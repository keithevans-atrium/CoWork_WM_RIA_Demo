snapshots:
  - name: snap_fsc_opportunity
    description: "SCD Type 2 history for opportunity changes (stage, amount, probability)"
    relation: ref('raw_opportunity')
    config:
      schema: SNAPSHOTS
      unique_key: record_pk
      strategy: check
      check_cols:
        - change_key
      hard_deletes: new_record

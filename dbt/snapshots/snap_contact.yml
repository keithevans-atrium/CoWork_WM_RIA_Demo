snapshots:
  - name: snap_contact
    description: "SCD Type 2 history for FSC contact changes (demographics, risk tolerance, employment)"
    relation: ref('brz_contact')
    config:
      schema: SNAPSHOTS
      unique_key: record_pk
      strategy: check
      check_cols:
        - change_key
      hard_deletes: new_record

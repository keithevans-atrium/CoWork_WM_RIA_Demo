snapshots:
  - name: snap_fsc_advisor_team
    description: "SCD Type 2 history for advisor team changes (lead, branch, region)"
    relation: ref('raw_advisor_team')
    config:
      schema: SNAPSHOTS
      unique_key: record_pk
      strategy: check
      check_cols:
        - change_key
      hard_deletes: new_record

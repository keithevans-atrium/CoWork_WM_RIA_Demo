snapshots:
  - name: snap_fee_billing
    description: "SCD Type 2 history for fee billing changes (amount, rate, status)"
    relation: ref('brz_fee_billing')
    config:
      schema: SNAPSHOTS
      unique_key: record_pk
      strategy: check
      check_cols:
        - change_key
      hard_deletes: new_record

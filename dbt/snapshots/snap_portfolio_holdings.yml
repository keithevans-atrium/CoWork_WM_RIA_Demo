snapshots:
  - name: snap_portfolio_holdings
    description: "SCD Type 2 history for holdings changes (quantity, value, weight, cost basis)"
    relation: ref('raw_portfolio_holdings')
    config:
      schema: SNAPSHOTS
      unique_key: record_pk
      strategy: check
      check_cols:
        - change_key
      hard_deletes: new_record
